// Load modules

var Chai = require('chai');
var Iron = process.env.TEST_COV ? require('../lib-cov') : require('../lib');


// Declare internals

var internals = {};


// Test shortcuts

var expect = Chai.expect;


describe('Iron', function () {

    it('should generate an encrypted token', function (done) {

        done();
    });
});

